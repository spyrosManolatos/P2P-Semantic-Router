services:
  clustered-bootstrap:
    environment:
      - CONFIG_PATH=/app/containerized_environment/config.vnodes.yaml
  clustered-node-1:
    environment:
      - CONFIG_PATH=/app/containerized_environment/config.vnodes.yaml
  clustered-node-2:
    environment:
      - CONFIG_PATH=/app/containerized_environment/config.vnodes.yaml
  clustered-node-3:
    environment:
      - CONFIG_PATH=/app/containerized_environment/config.vnodes.yaml
  clustered-node-4:
    environment:
      - CONFIG_PATH=/app/containerized_environment/config.vnodes.yaml
  clustered-node-5:
    environment:
      - CONFIG_PATH=/app/containerized_environment/config.vnodes.yaml
  clustered-runner:
    environment:
      - CONFIG_PATH=/app/containerized_environment/config.vnodes.yaml
